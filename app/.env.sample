# Legacy Flask configuration retained for the original app module.
# The active production storefront uses backend/.env and MongoDB.
PAYSTACK_PUBLIC_KEY=your-public-key-here
PAYSTACK_SECRET_KEY=your-secret-key-here
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/skincare_with_happy
DATABASE=database.db
SECRET_KEY=replace-with-a-long-random-secret
