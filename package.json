{
  "name": "skincare-with-happy",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "frontend:serve": "npx serve frontend",
    "backend:start": "npm --prefix backend start",
    "backend:dev": "npm --prefix backend run dev"
  }
}
